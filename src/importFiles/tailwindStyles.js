const inputs = "h-16px";
const icons = "mr-2 inline-block h-5 w-5 align-[-2px] text-[var(--textColor)]";

const tailwindClass = {
    inputs: inputs,
    icons: icons,
}

export {inputs, tailwindClass};